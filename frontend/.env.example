# Copy this file to .env.local and override if your backend runs elsewhere.
# When using cloudflared / ngrok, you typically only need to change this one value.

VITE_API_BASE=http://localhost:5000/api
